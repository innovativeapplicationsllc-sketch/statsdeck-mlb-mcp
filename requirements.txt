# Auto-mirrored from pyproject.toml [project.dependencies].
# Exists so Railway's Nixpacks installs dependencies explicitly via
#   pip install -r requirements.txt
# instead of a 'pip install .' layer that Railway's build cache kept serving
# stale (psycopg never got installed). Keep this in sync with pyproject.toml.
mcp[cli]>=1.0.0
pybaseball>=2.2.7
httpx>=0.27.0
diskcache>=5.6.3
python-dateutil>=2.9.0
pydantic>=2.0.0
uvicorn>=0.27.0
starlette>=0.36.0
redis>=5.0.0
PyJWT[crypto]>=2.8.0
psycopg[binary]>=3.1
